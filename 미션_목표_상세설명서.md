# B1-2 미션 목표 상세 설명서

## 1. 이 미션을 왜 하는가

개발자는 보통 코드를 작성하고 기능이 잘 동작하는지 확인하는 데 익숙합니다. 하지만 실제 서비스에서는 코드가 운영체제 위에서 하나의 프로세스로 실행되고, 그 프로세스는 메모리, CPU, 스레드, 파일, 네트워크 포트 같은 시스템 자원을 계속 사용합니다.

기능 테스트에서는 문제가 없어 보이던 프로그램도 운영 중에는 다음과 같은 장애를 일으킬 수 있습니다.

- 메모리를 계속 사용하다가 한계에 도달해 종료된다.
- CPU를 과도하게 사용해 시스템 응답이 느려진다.
- 여러 스레드가 서로의 자원을 기다리며 멈춘다.

이 미션의 핵심은 단순히 “프로그램이 죽었다” 또는 “멈췄다”라고 말하는 것이 아닙니다. 관제 데이터, 실행 로그, `ps`, `top` 같은 시스템 도구의 출력으로 장애를 관찰하고, 그 증거를 바탕으로 원인을 추론한 뒤, 동료 개발자가 이해할 수 있는 기술 리포트로 정리하는 것입니다.

운영 환경에서 가장 위험한 대응은 원인을 확인하지 않고 재시작부터 하는 것입니다. 재시작은 서비스를 잠시 살릴 수 있지만, 장애 원인을 함께 지워버릴 수 있습니다. 이 실습은 장애가 발생했을 때 “무엇을 먼저 봐야 하는가”, “어떤 증거가 원인을 설명하는가”, “어떻게 리포트로 남겨야 같은 문제가 반복되지 않는가”를 연습하기 위해 설계되었습니다.

## 2. 최종적으로 설명할 수 있어야 하는 것

이 미션을 끝낸 뒤에는 다음 내용을 스스로 설명할 수 있어야 합니다.

- 메모리 누수가 왜 프로세스 종료나 OOM으로 이어지는가
- CPU 과점유가 왜 전체 서비스 지연으로 이어질 수 있는가
- Deadlock이 무엇이고, 왜 프로세스가 살아 있는데도 일을 하지 못하는가
- 로그와 관제 데이터를 근거로 장애 원인을 어떻게 추론하는가
- GitHub Issue 형식으로 장애 리포트를 어떻게 작성하는가

여기서 중요한 것은 정답 문장을 외우는 것이 아닙니다. 관찰한 현상에서 출발해 원인으로 거슬러 올라가는 사고 흐름을 익히는 것입니다.

### 2.1 메모리 누수가 왜 프로세스 종료나 OOM으로 이어지는가

메모리 누수는 프로그램이 필요 없어진 데이터를 계속 메모리에 남겨두는 현상입니다. 처음에는 작은 데이터라서 문제가 없어 보이지만, 프로그램이 오래 실행되거나 요청이 많이 들어오면 메모리 사용량이 계속 쌓입니다.

운영체제는 각 프로세스가 사용하는 메모리를 관리합니다. 프로세스가 너무 많은 메모리를 사용하면 다른 프로세스가 사용할 메모리가 부족해지고, 시스템 전체가 느려지거나 불안정해질 수 있습니다. 그래서 운영체제나 애플리케이션 내부 보호 장치는 일정 기준을 넘은 프로세스를 종료할 수 있습니다.

이번 실습에서는 운영체제의 OOM Killer가 직접 개입하기 전에, 앱 내부의 `MemoryGuard`가 먼저 동작합니다. `MEMORY_LIMIT`으로 정한 값보다 Heap 사용량이 커지면 앱은 “시스템을 더 위험하게 만들기 전에 나 자신을 종료한다”는 방식으로 동작합니다.

초보자가 설명할 때는 다음 흐름으로 말하면 좋습니다.

```text
메모리 누수는 필요 없는 데이터를 계속 붙잡는 현상이다.
시간이 지날수록 프로세스의 메모리 사용량이 증가한다.
사용량이 한도를 넘으면 시스템 안정성을 위해 프로세스가 종료될 수 있다.
이번 실습에서는 MemoryGuard가 MEMORY_LIMIT 초과를 감지하고 앱을 종료했다.
```

확인해야 할 증거는 `Current Heap`, `RSS`, `Memory limit exceeded`, `Self-terminating process` 같은 로그입니다. 특히 `MEMORY_LIMIT`을 올렸을 때 생존 시간이 늘어난다면, 메모리 한도와 종료 현상이 연결되어 있다는 강한 근거가 됩니다.

### 2.2 CPU 과점유가 왜 전체 서비스 지연으로 이어질 수 있는가

CPU는 프로그램이 실제 계산을 수행할 때 사용하는 자원입니다. 한 서버에는 여러 프로세스가 동시에 실행되지만, CPU는 무한하지 않습니다. 운영체제는 여러 프로세스가 CPU를 나누어 쓰도록 스케줄링합니다.

특정 프로세스가 CPU를 과도하게 사용하면 다른 프로세스가 CPU를 배정받기까지 더 오래 기다려야 합니다. 웹 서버라면 요청 응답이 늦어지고, 로그 수집기나 모니터링 에이전트도 제때 실행되지 못할 수 있습니다. 그래서 CPU 장애는 단순히 “한 프로그램이 바쁘다”가 아니라 “서버 전체 응답성이 나빠질 수 있다”는 문제로 봐야 합니다.

이번 실습에서는 `CPU_MAX_OCCUPY` 값을 높게 설정했을 때 `CpuWorker`의 부하가 계속 상승했고, 임계치를 넘자 `CPU Threshold Violated` 로그가 발생했습니다. 반대로 `CPU_MAX_OCCUPY`를 낮게 설정하면 부하가 일정 수준에서 쿨다운되며 안정적으로 유지됩니다.

초보자가 설명할 때는 다음 흐름이 좋습니다.

```text
CPU는 여러 프로세스가 나눠 쓰는 계산 자원이다.
하나의 프로세스가 CPU를 오래 사용하면 다른 작업이 기다리게 된다.
그 결과 요청 처리 지연, 모니터링 지연, 시스템 응답 저하가 발생할 수 있다.
이번 실습에서는 CpuWorker 부하가 상승하다가 보호 임계치를 넘어서 종료되었다.
```

CPU 분석에서는 `top`이나 `ps` 출력만 보지 말고 앱 로그도 함께 봐야 합니다. 컨테이너 환경이나 앱 내부 시뮬레이션 방식 때문에 OS에서 보이는 `%CPU`와 앱 내부 부하 지표가 다를 수 있기 때문입니다. 중요한 것은 “어떤 지표가 상승했고, 그 상승 직후 어떤 보호 로그가 남았는가”입니다.

### 2.3 Deadlock이 무엇이고, 왜 프로세스가 살아 있는데도 일을 하지 못하는가

Deadlock은 여러 스레드가 서로 상대방이 가진 자원을 기다리면서 아무도 앞으로 나아가지 못하는 상태입니다. 프로세스가 종료된 것은 아니기 때문에 `ps`로 보면 PID가 살아 있습니다. 하지만 실제 작업은 더 이상 진행되지 않습니다.

예를 들어 Thread-1이 A 자원을 잡고 B 자원을 기다리고, Thread-2가 B 자원을 잡고 A 자원을 기다린다고 생각하면 됩니다. 둘 다 상대방이 가진 자원을 필요로 하지만, 누구도 먼저 놓지 않기 때문에 영원히 기다립니다.

이번 실습에서는 다음 구조가 로그에 나타납니다.

```text
Worker-Thread-1: Shared_Memory_A를 획득한 뒤 Socket_Pool_B를 기다림
Worker-Thread-2: Socket_Pool_B를 획득한 뒤 Shared_Memory_A를 기다림
```

이 상태에서는 CPU 사용률이 높지 않을 수 있습니다. 오히려 스레드들이 계산을 하는 것이 아니라 기다리고 있기 때문에 CPU가 낮게 보입니다. 그래서 Deadlock은 CPU Spike와 반대로 “조용한 장애”처럼 보일 수 있습니다. 프로세스는 살아 있고, CPU도 낮고, 메모리도 변하지 않지만, 로그와 작업 진행은 멈춰 있습니다.

초보자가 설명할 때는 다음 흐름이 좋습니다.

```text
Deadlock은 여러 스레드가 서로의 자원을 기다리며 멈춘 상태다.
프로세스는 종료되지 않기 때문에 PID는 계속 보인다.
하지만 스레드들이 BLOCKED 상태로 기다리기 때문에 실제 작업은 진행되지 않는다.
이번 실습에서는 두 스레드가 Shared_Memory_A와 Socket_Pool_B를 서로 기다리는 순환 대기 상태였다.
```

Deadlock을 판단할 때는 PID 존재 여부, 스레드 수, CPU/RSS 정체, 마지막 로그의 `WAITING` 또는 `BLOCKED` 메시지를 함께 봐야 합니다.

### 2.4 로그와 관제 데이터를 근거로 장애 원인을 어떻게 추론하는가

장애 분석은 추측이 아니라 증거를 연결하는 작업입니다. 로그 하나만 보고 결론을 내리면 위험합니다. 반대로 숫자만 보고 앱 내부 상황을 무시해도 잘못 판단할 수 있습니다.

좋은 분석은 보통 다음 구조를 가집니다.

```text
현상: 프로세스가 종료되었다.
관제: 종료 전 RSS가 계속 증가했다.
앱 로그: MemoryGuard가 MEMORY_LIMIT 초과를 기록했다.
비교 실험: MEMORY_LIMIT을 올리자 종료 시점이 늦어졌다.
결론: 메모리 누수로 인한 MemoryGuard 종료 가능성이 높다.
```

CPU와 Deadlock도 같은 방식으로 접근합니다.

CPU의 경우:

```text
현상: 프로세스가 CPU 임계 위반 로그를 남기고 종료되었다.
앱 로그: CpuWorker Current Load가 점진적으로 상승했다.
비교 실험: CPU_MAX_OCCUPY를 낮추자 쿨다운이 동작했다.
결론: CPU 과점유 보호 정책이 발동한 것으로 판단한다.
```

Deadlock의 경우:

```text
현상: 프로세스는 살아 있지만 로그가 멈췄다.
관제: CPU와 RSS가 거의 변하지 않는다.
스레드: 여러 스레드가 존재하지만 실행 중인 작업이 없다.
앱 로그: 두 스레드가 서로의 자원을 WAITING/BLOCKED 상태로 기다린다.
결론: 순환 대기에 의한 Deadlock으로 판단한다.
```

이처럼 원인 분석은 “증거 A 때문에 원인 B다”가 아니라, 여러 증거가 같은 방향을 가리키는지 확인하는 과정입니다.

### 2.5 GitHub Issue 형식으로 장애 리포트를 어떻게 작성하는가

GitHub Issue 형식의 장애 리포트는 협업을 위한 문서입니다. 장애를 겪은 사람만 이해하는 기록이 아니라, 나중에 다른 개발자나 운영자가 읽어도 같은 상황을 재현하고 판단할 수 있어야 합니다.

좋은 장애 리포트에는 다음 네 가지가 반드시 들어가야 합니다.

- Description: 어떤 현상이 발생했는가
- Evidence & Logs: 그 현상을 뒷받침하는 증거는 무엇인가
- Root Cause Analysis: 증거를 바탕으로 추론한 원인은 무엇인가
- Workaround & Verification: 어떤 조치를 했고, 결과가 어떻게 달라졌는가

초보자가 가장 자주 하는 실수는 “원인 같습니다”라고만 쓰는 것입니다. 예를 들어 “메모리 문제 같습니다”라고 쓰면 읽는 사람은 왜 그렇게 판단했는지 알 수 없습니다. 대신 다음처럼 써야 합니다.

```text
monitor 로그에서 RSS가 21.0MB -> 46.0MB -> 71.0MB로 증가했다.
앱 로그에서 Current Heap이 25MB -> 50MB -> 75MB로 증가했다.
이후 MemoryGuard가 "Memory limit exceeded (75MB >= 64MB)"를 남기고 종료했다.
따라서 메모리 증가가 MEMORY_LIMIT 초과로 이어져 프로세스가 종료된 것으로 판단한다.
```

이런 방식으로 쓰면 동료는 리포트만 보고도 다음 행동을 결정할 수 있습니다. 예를 들어 임시로 메모리 한도를 올릴지, 누수 코드 수정을 우선할지, 재현 환경을 다시 돌릴지 판단할 수 있습니다.

### 2.6 스스로 설명할 수 있다는 것의 기준

이 미션의 목표는 전문 용어를 많이 쓰는 것이 아닙니다. 오히려 어려운 현상을 쉬운 말로 정확하게 설명할 수 있어야 합니다.

다음 질문에 답할 수 있으면 목표에 가까워진 것입니다.

- 왜 메모리 사용량이 계속 증가하면 위험한가?
- `MEMORY_LIMIT`을 올렸는데도 근본 해결이 아닌 이유는 무엇인가?
- CPU 사용률이 높으면 왜 다른 요청이 느려질 수 있는가?
- Deadlock은 왜 프로세스가 살아 있는데도 장애인가?
- `ps`, `top`, 앱 로그, monitor 로그는 각각 어떤 증거를 제공하는가?
- Before & After 비교는 왜 장애 리포트에서 중요한가?

최종적으로는 다음처럼 짧게 요약할 수 있어야 합니다.

```text
이 미션은 앱 장애를 로그와 시스템 관제 데이터로 분석하는 훈련이다.
OOM은 메모리 증가와 한도 초과 로그로 판단하고,
CPU 장애는 부하 상승과 보호 정책 로그로 판단하며,
Deadlock은 PID가 살아 있지만 스레드가 서로의 자원을 기다리는 정체 상태로 판단한다.
분석 결과는 GitHub Issue 형식으로 남겨 재현, 조치, 재발 방지에 활용한다.
```

## 3. 운영체제 관점에서 프로그램 바라보기

우리가 실행하는 프로그램은 운영체제 입장에서는 프로세스입니다. 프로세스는 실행되는 동안 여러 자원을 사용합니다.

- 메모리: 변수, 객체, 캐시, 버퍼 등이 올라가는 공간
- CPU: 실제 계산과 명령 실행을 담당하는 자원
- 스레드: 하나의 프로세스 안에서 동시에 또는 번갈아 실행되는 작업 흐름
- 파일/디렉터리: 로그, 설정, 업로드 파일, 키 파일 등이 저장되는 공간
- 포트: 외부 요청을 받기 위해 열어두는 네트워크 입구

이번 실습의 `agent-app-leak`도 단순한 파일이 아니라 실행되는 순간 하나의 프로세스가 됩니다. 그래서 앱 로그만 보는 것으로는 부족합니다. 앱 내부 로그와 운영체제 관제 데이터를 함께 봐야 실제 장애의 모양이 보입니다.

예를 들어 앱 로그에는 `Current Heap: 75MB`라고 나오고, 관제 로그에는 `RSS:71.0MB`처럼 보일 수 있습니다. 두 값은 완전히 같은 개념은 아니지만, 둘 다 메모리 사용량이 증가하고 있다는 같은 방향의 증거가 됩니다.

## 4. Memory Leak과 OOM Crash

### 4.1 Memory Leak이란

Memory Leak은 프로그램이 더 이상 필요하지 않은 메모리를 계속 붙잡고 있는 상태입니다. 초보자 관점에서는 “청소하지 않고 계속 쌓아두는 상태”라고 이해하면 됩니다.

예를 들어 요청을 처리할 때마다 데이터를 리스트에 추가하지만, 처리가 끝난 뒤에도 그 데이터를 지우지 않는다고 가정해 봅시다. 요청이 10개일 때는 괜찮지만, 10만 개, 100만 개가 되면 메모리는 계속 증가합니다.

메모리 누수의 무서운 점은 처음에는 정상처럼 보인다는 것입니다. CPU도 낮고 응답도 잘 될 수 있습니다. 하지만 시간이 지나면 메모리 사용량이 꾸준히 올라가고, 결국 프로세스나 시스템이 한계에 도달합니다.

### 4.2 OOM과 MemoryGuard

OOM은 Out Of Memory의 약자로, 사용할 수 있는 메모리가 부족한 상황입니다. 실제 운영체제에서는 메모리가 부족하면 OOM Killer가 프로세스를 종료할 수도 있습니다.

이번 실습의 앱은 그 전에 자체 보호 정책인 `MemoryGuard`를 가지고 있습니다. 설정된 `MEMORY_LIMIT`을 넘으면 시스템 전체 불안정을 막기 위해 스스로 종료합니다.

중요한 관찰 포인트는 다음입니다.

- 시간이 지날수록 메모리 사용량이 증가하는가
- 증가가 일시적인가, 계속 누적되는가
- 종료 직전에 `Memory limit exceeded` 로그가 있는가
- `MEMORY_LIMIT`을 올렸을 때 생존 시간이 늘어나는가

이번 실습에서는 `MEMORY_LIMIT=64`일 때 약 8초 후 종료되었고, `MEMORY_LIMIT=128`로 올렸을 때 약 17초까지 생존했습니다. 이 비교는 “메모리 한도가 장애 발생 시점에 직접 영향을 준다”는 좋은 증거입니다.

### 4.3 학습 포인트

메모리 장애를 분석할 때는 “메모리가 많으면 해결된다”라고 끝내면 안 됩니다. 한도를 올리는 것은 임시 조치입니다. 메모리가 계속 증가하는 원인이 그대로라면 언젠가는 더 큰 한도도 다시 넘게 됩니다.

근본적인 해결은 다음 방향에서 찾아야 합니다.

- 불필요한 객체를 계속 참조하고 있지 않은가
- 캐시 크기에 제한이 있는가
- 요청별 임시 데이터가 처리 후 해제되는가
- 장시간 실행 시 메모리 증가 패턴이 안정화되는가

## 5. CPU Spike와 Latency

### 5.1 CPU 과점유란

CPU 과점유는 특정 프로세스나 작업이 CPU를 지나치게 많이 사용하는 상태입니다. CPU는 여러 프로세스가 나눠 쓰는 자원이기 때문에, 한 프로세스가 오래 붙잡고 있으면 다른 작업은 기다려야 합니다.

웹 서버라면 요청 처리가 늦어지고, 배치 시스템이라면 작업 완료 시간이 길어지고, 운영 서버라면 관제나 로그 처리까지 지연될 수 있습니다.

### 5.2 CPU Spike가 위험한 이유

CPU 사용률이 순간적으로 올라가는 것 자체가 항상 장애는 아닙니다. 짧은 계산 작업이나 배치 작업에서는 정상일 수도 있습니다. 문제는 다음과 같은 경우입니다.

- CPU가 지속적으로 높게 유지된다.
- 특정 프로세스 하나가 CPU를 독점한다.
- CPU 상승과 함께 응답 지연 또는 종료 로그가 발생한다.
- 보호 정책이 임계 초과로 프로세스를 종료한다.

이번 실습에서는 `CPU_MAX_OCCUPY=100`으로 실행했을 때 앱 내부 `CpuWorker` 부하가 점진적으로 상승했고, 53.58%에서 `CPU Threshold Violated` 로그가 발생했습니다.

반대로 `CPU_MAX_OCCUPY=10`에서는 10%에 도달한 뒤 쿨다운이 동작하며 안정적으로 유지되었습니다. 이것은 CPU 한도 조정이 장애 회피에 영향을 준다는 비교 증거입니다.

### 5.3 ps/top과 앱 내부 지표의 차이

이번 실습에서 주의할 점이 하나 있습니다. Docker 환경에서 `ps`나 `top`으로 본 CPU 사용률은 앱 내부 `CpuWorker` 로그와 다르게 보일 수 있습니다.

이 차이는 다음 이유로 발생할 수 있습니다.

- 앱 내부 부하 지표가 실제 OS `%CPU`와 완전히 같은 값이 아닐 수 있다.
- 컨테이너 환경에서는 CPU 계산 기준이 호스트와 다를 수 있다.
- 프로세스 구조상 부모/자식 프로세스가 나뉘어 관측될 수 있다.

따라서 CPU 리포트에서는 `top` 출력만 보지 말고, 앱 내부 로그와 함께 해석해야 합니다. 좋은 장애 분석은 하나의 숫자만 믿는 것이 아니라, 여러 증거를 서로 맞춰 보는 과정입니다.

### 5.4 학습 포인트

CPU 장애 분석에서 중요한 질문은 다음입니다.

- CPU가 언제부터 상승했는가
- 어떤 프로세스 또는 스레드가 사용하는가
- 상승이 일시적인가, 지속적인가
- 앱 로그에 보호 정책 또는 종료 원인이 남았는가
- 한도를 조정했을 때 동작이 달라지는가

CPU 장애는 단순히 “서버 사양이 부족하다”로 결론내리기 쉽습니다. 하지만 실제로는 무한 루프, 비효율적인 알고리즘, 과도한 동시성, 잘못된 재시도 로직, 제한 없는 작업 큐가 원인일 수 있습니다.

## 6. Deadlock

### 6.1 Deadlock이란

Deadlock은 여러 작업이 서로 상대방이 가진 자원을 기다리며 영원히 진행하지 못하는 상태입니다.

이번 실습에서는 다음 상황이 발생했습니다.

- Worker-Thread-1은 `Shared_Memory_A`를 잡고 있다.
- Worker-Thread-2는 `Socket_Pool_B`를 잡고 있다.
- Thread-1은 작업을 끝내려면 `Socket_Pool_B`가 필요하다.
- Thread-2는 로그를 쓰려면 `Shared_Memory_A`가 필요하다.

결과적으로 두 스레드는 서로가 가진 자원을 기다리며 멈춥니다. 프로세스는 살아 있지만 실제 일은 진행되지 않습니다.

### 6.2 Deadlock의 4대 조건

Deadlock은 보통 다음 네 조건이 동시에 만족될 때 발생합니다.

- 상호 배제: 하나의 자원을 한 번에 하나의 스레드만 사용할 수 있다.
- 점유 대기: 자원을 가진 상태에서 다른 자원을 기다린다.
- 비선점: 다른 스레드가 가진 자원을 강제로 빼앗을 수 없다.
- 순환 대기: A는 B를 기다리고, B는 A를 기다리는 원형 대기 구조가 생긴다.

이번 실습 로그는 이 네 조건을 매우 명확하게 보여줍니다.

```text
Worker-Thread-1 LOCK ACQUIRED: Shared_Memory_A
Worker-Thread-2 LOCK ACQUIRED: Socket_Pool_B
Worker-Thread-2 WAITING for Shared_Memory_A
Worker-Thread-1 WAITING for Socket_Pool_B
```

이 로그만으로도 순환 대기를 추론할 수 있습니다.

### 6.3 Deadlock은 왜 찾기 어려운가

Deadlock은 프로세스가 종료되지 않는 경우가 많습니다. 그래서 단순히 “프로세스가 있나?”만 확인하면 정상처럼 보일 수 있습니다.

Deadlock을 의심해야 하는 신호는 다음입니다.

- PID는 살아 있다.
- CPU 사용률이 거의 없다.
- 메모리 사용량도 변하지 않는다.
- 로그가 특정 지점 이후 멈춘다.
- 스레드는 존재하지만 진행 상태가 없다.

이번 실습에서도 `ps`로 PID가 존재했고, `ps -L`에서 여러 스레드가 보였지만, `top -H` 기준으로 실행 중인 스레드는 없고 모두 sleep 상태였습니다. 이것은 프로세스가 살아 있으나 작업이 진행되지 않는다는 중요한 증거입니다.

### 6.4 학습 포인트

Deadlock의 근본 해결은 보통 다음 방법으로 접근합니다.

- 모든 스레드가 자원을 같은 순서로 획득하게 만든다.
- 락 획득에 타임아웃을 둔다.
- 실패 시 이미 잡은 락을 풀고 재시도한다.
- 공유 자원 범위를 줄인다.
- 가능한 경우 락 없는 구조나 메시지 큐로 바꾼다.

이번 실습의 임시 조치는 `MULTI_THREAD_ENABLE=false`입니다. 멀티스레드 동시 실행을 끄면 Deadlock 경로 자체를 회피할 수 있습니다. 하지만 실제 서비스에서는 성능 요구 때문에 멀티스레드를 완전히 끌 수 없는 경우가 많으므로, 락 설계 자체를 수정하는 것이 근본 해결입니다.

## 7. 왜 GitHub Issue 형식으로 리포트를 쓰는가

장애 분석은 혼자 알고 끝나는 일이 아닙니다. 운영 장애는 재발 방지와 협업이 중요합니다. GitHub Issue 형식은 다음 장점이 있습니다.

- 현상, 증거, 원인, 조치를 구조적으로 남길 수 있다.
- 동료가 같은 문제를 빠르게 이해할 수 있다.
- 추후 같은 장애가 발생했을 때 검색 가능한 기록이 된다.
- 임시 조치와 근본 해결 과제를 분리할 수 있다.

좋은 리포트는 감상이 아니라 증거 중심이어야 합니다.

나쁜 예:

```text
앱이 이상하게 죽었습니다. 메모리 문제 같습니다.
```

좋은 예:

```text
10:48:51 RSS 46.0MB, 10:48:54 RSS 71.0MB로 증가했고,
10:48:57 MemoryGuard가 "Memory limit exceeded (75MB >= 64MB)" 로그를 남긴 뒤 종료했습니다.
MEMORY_LIMIT=128로 변경하자 종료 시점이 10:49:20으로 늦춰졌습니다.
```

두 문장의 차이는 명확합니다. 좋은 리포트는 다른 사람이 재현하고 판단할 수 있는 정보를 제공합니다.

## 8. 실습을 통해 익혀야 하는 분석 순서

장애를 만났을 때 다음 순서로 생각하면 좋습니다.

1. 현상을 확인한다.
   - 종료인지, 지연인지, 멈춤인지 구분한다.

2. 프로세스 상태를 확인한다.
   - `ps`, `top`, `ps -L`, `top -H`로 PID와 스레드 상태를 본다.

3. 앱 로그를 확인한다.
   - 마지막 로그, CRITICAL/WARNING 로그, 보호 정책 로그를 찾는다.

4. 관제 로그와 연결한다.
   - CPU, RSS, 스레드 수, 경과 시간이 어떻게 변했는지 본다.

5. 환경변수를 하나씩 바꿔 비교한다.
   - 한 번에 여러 조건을 바꾸면 원인을 분리하기 어렵다.

6. Before & After를 기록한다.
   - 조치 전후 수치와 로그를 나란히 남긴다.

7. 임시 조치와 근본 해결을 구분한다.
   - 환경변수 조정은 임시 완화이고, 코드/설계 수정이 근본 해결일 수 있다.

## 9. 이번 산출물의 의미

이번 작업 영역에는 다음 산출물이 있다.

- `reports/01-oom-crash.md`: 메모리 증가와 MemoryGuard 종료 분석
- `reports/02-cpu-latency.md`: CPU 부하 증가와 임계 위반 분석
- `reports/03-deadlock.md`: 락 순환 대기와 무응답 상태 분석
- `artifacts/`: 실제 실행 로그와 관제 로그
- `Dockerfile`, `docker-compose.yml`: 격리된 실습 환경
- `monitor.sh`: 프로세스 자원 관제 도구
- `run-agent-case.sh`: 케이스별 실행 및 로그 수집 도구

이 산출물은 단순 제출용 파일이 아니라, 장애를 관찰하고 분석하는 훈련의 흔적입니다. 특히 `artifacts/`의 로그를 직접 열어 리포트와 대조해 보면, “증거에서 결론으로 가는 과정”을 더 잘 이해할 수 있습니다.

## 10. 더 깊게 생각해 볼 질문

실습을 끝낸 뒤 다음 질문에 답해보면 학습이 훨씬 깊어집니다.

- 메모리 한도를 512MB로 올리는 것은 해결인가, 완화인가?
- CPU 한도를 너무 낮추면 어떤 부작용이 있을까?
- Deadlock을 피하기 위해 멀티스레드를 끄면 성능에는 어떤 영향이 있을까?
- 로그가 없었다면 OOM, CPU, Deadlock을 어떻게 구분할 수 있을까?
- 운영 서버에서 장애가 났을 때 재시작 전에 반드시 수집해야 할 정보는 무엇일까?
- 같은 장애가 다시 발생하지 않게 하려면 Issue 이후 어떤 개발 작업이 필요할까?

이 질문에 답할 수 있다면, 단순히 미션을 수행한 수준을 넘어 실제 운영 장애 분석의 사고방식에 가까워진 것입니다.
